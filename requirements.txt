fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
motor>=3.3.2
beanie>=1.26.0
python-multipart>=0.0.20
email-validator>=2.2.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
cloudinary>=1.41.0
openpyxl>=3.1.2
# F-COBRANZA-043 (2026-07-22): export PDF para Reporte de Caja
reportlab>=4.0.0
pymongo<=4.6.3
# TECH-004: error tracking en producción (opcional; solo se activa si SENTRY_DSN está configurado)
sentry-sdk[fastapi]>=2.0.0
# TECH-003: Server-Sent Events para notificaciones en tiempo real (reemplaza polling)
sse-starlette>=2.0.0
# TECH-001: framework de tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
# CERT-002 (2026-07-30): cliente HTTP async para signed URL fallback
# (descargar PDFs de Cloudinary con autenticación cuando el asset es legacy privado)
httpx>=0.27.0

# F-CERT-NO-DEUDOR-COBRO (2026-08-17): el Certificado de No Deudor se imprime
# sobre la hoja membretada de la Unidad. Los membretes son PDF solo graficos
# (0 texto extraible), asi que el cuerpo se genera con reportlab y se
# superpone con pypdf. Ya venia instalado como dependencia transitiva; se
# declara explicito porque ahora el codigo depende de el.
pypdf>=5.0.0
